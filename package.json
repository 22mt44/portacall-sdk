{
	"name": "portacall-sdk",
	"private": true,
	"packageManager": "bun@1.3.4",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "turbo build",
		"test": "turbo test",
		"typecheck": "turbo typecheck",
		"lint": "turbo lint && biome check package.json README.md turbo.json .github/workflows/ci.yml",
		"check": "bun run test && bun run typecheck && bun run lint",
		"release:client:dry": "npm publish --workspace @portacall/client --dry-run",
		"release:client": "npm publish --workspace @portacall/client",
		"release:react:dry": "npm publish --workspace @portacall/react --dry-run",
		"release:react": "npm publish --workspace @portacall/react",
		"release:proxy:dry": "npm publish --workspace @portacall/proxy --dry-run",
		"release:proxy": "npm publish --workspace @portacall/proxy"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.8",
		"@types/bun": "^1.3.11",
		"turbo": "^2.5.8",
		"tsdown": "^0.21.4",
		"typescript": "^5.9.3"
	}
}
